---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  labels:
    grafana_dashboard: "1"
data:
  {{- $files := .Files }}
  {{- range .Values.dashboards }}
  {{ . }}: |-
{{ $files.Get (print "dashboards/" .) | indent 4 }}
  {{- end }}

