apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-dashboards
  namespace: argocd
spec:
  project: infra
  source:
    repoURL: https://github.com/sdelrio/k8s-gitops.git
    targetRevision: feature/gitopstest
    path: deployments/nginx-dashboards
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
