repositories:
  - name: chartcenter
    url: https://repo.chartcenter.io

releases:
- name: metallb-config
  chart: chartcenter/incubator/raw
  namespace: metallb-system
  wait: true
  version: "0.2.3"
  values:
    - environments/{{ .Environment.Name }}/metallb/resources.yaml
    - environments/all/metallb/templates.yaml.gotmpl
  labels:
    tier: loadbalancer

- name: metallb-system
  namespace: metallb-system
  createNamespace: true
  wait: true
  chart: ./metallb
  needs:
    - metalllb-config
  hooks:
    - events:
        - prepare
        - cleanup
      command: "./helmify.sh"
      args: ["{{`{{if eq .Event.Name \"prepare\"}}build{{else}}clean{{end}}`}}", "{{`{{.Release.Chart}}`}}", "{{`{{.Environment.Name}}`}}"]
  labels:
    tier: loadbalancer

- name: nginx-internal
  chart: chartcenter/kubernetes-ingress-nginx/ingress-nginx
  namespace: kube-system
  createNamespace: true
  wait: false
  version: '{{ .Values | get "nginx_int_version" "2.1.13" }}'
  values:
    - environments/{{ .Environment.Name }}/nginx/internal.yaml
  labels:
    tier: ingress
    scope: internal

- name: nginx-external
  chart: chartcenter/kubernetes-ingress-nginx/ingress-nginx
  namespace: kube-system
  createNamespace: true
  wait: false
  version: '{{ .Values | get "nginx_ext_version" "2.1.13" }}'
  values:
    - environments/{{ .Environment.Name }}/nginx/external.yaml
  labels:
    tier: ingress
    scope: external

- name: cert-manager
  namespace: cert-manager
  createNamespace: true
  wait: true
  chart: chartcenter/jetstack/cert-manager
  version: '{{ .Values | get "certmanager_version" "v1.0.4" }}'
  values:
    - installCRDs: true
  labels:
    tier: cert
    scope: manager

- name: cert-issuers
  chart: chartcenter/incubator/raw
  namespace: cert-manager
  version: "0.2.3"
  disableValidation: true
  values:
    - environments/{{ .Environment.Name }}/cert-manager/templates.yaml.gotmpl
  labels:
    tier: cert
    scope: issuers

- name: hostpath
  namespace: openebs
  createNamespace: true
  wait: true
  chart: chartcenter/openebs/openebs
  version: '{{ .Values | get "openebs_version" "2.2.1" }}'
  values:
    - environments/{{ .Environment.Name }}/openebs/values.yaml
  labels:
    tier: storage
    scope: openebs

- name: hostpath-sc
  chart: chartcenter/incubator/raw
  namespace: cert-manager
  version: "0.2.3"
#  disableValidation: true
  values:
    - environments/{{ .Environment.Name }}/openebs/sc-openebs-hostpath.yaml
  labels:
    tier: storage
    scope: class

- name: longhorn
  namespace: longhorn
  createNamespace: true
  wait: true
  chart: chartcenter/longhorn/longhorn
  version: '{{ .Values | get "longhorn_version" "1.0.2" }}'
  values:
    - environments/{{ .Environment.Name }}/longhorn/values.yaml
  labels:
    tier: storage
    scope: longhorn


- name: velero-config
  chart: chartcenter/incubator/raw
  createNamespace: true
  namespace: velero
  wait: true
  version: "0.2.3"
  values:
    - environments/all/velero/templates.yaml.gotmpl
  labels:
    tier: velero

- name: velero-system
  namespace: velero
  createNamespace: true
  disableValidation: true
  chart: chartcenter/vmware-tanzu/velero
  version: '{{ .Values | get "velero_version" "2.13.6" }}'
  values:
    - environments/{{ .Environment.Name }}/velero/values.yaml.gotmpl
  labels:
    tier: velero

environments:
  dev:
    secrets:
      - environments/dev/secrets.yaml
    values:
      - nginx_int_version: '2.12.1'
      - nginx_ext_version: '2.11.3'
      - certmanager_version: 'v1.0.4'
      - openebs_version: '2.0.0'
      - longhorn_version: '1.0.2'
      - velero_version: '2.8.10'
  nuc:
    secrets:
      - environments/nuc/secrets.yaml
    values:
      - nginx_int_version: '2.11.3'
      - nginx_ext_version: '2.15.0'
      - certmanager_version: 'v0.16.0'
      - openebs_version: '2.0.0'
      - longhorn_version: '1.0.2'
      - velero_version: '2.8.2'
